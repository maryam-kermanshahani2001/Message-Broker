/* Unused but we can use it if we develop the code
package Server.model;

public enum MessageType {
    CONSUMER_REQUEST,
    CONSUMER_RESPONSE,
    PRODUCER_REQUEST,
    PRODUCER_RESPONSE,
    PING,PONG,PUBLISH
}
*/
